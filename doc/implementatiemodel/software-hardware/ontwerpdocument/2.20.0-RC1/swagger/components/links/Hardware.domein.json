{
  "operationId": "HardwareGET",
  "parameters": {
    "id": "$response.body#/Hardware.domein/@id"
  },
  "description": "De waarde van het attribuut `@id` kan gebruikt worden om het gerefereerde object van het type `HardwareDomein` op te halen."
}